{
  "name": "clientesapp-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:app": "npm run start --workspace mobile-web",
    "dev:api": "npm run dev --workspace api",
    "start": "npm run dev:app",
    "lint": "npm run lint --workspace api"
  }
}
